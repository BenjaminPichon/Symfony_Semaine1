{% extends 'base.html.twig' %}

{% block title %}Hello TaskController!{% endblock %}

{% block body %}
<div class="container">
    {% for task in Tasks%}
        <div class="card">
            <div class="card-body">
                <h2>{{task.name}}<h2>
            </div>
        </div>
    {% endfor %}

</div>
{% endblock %}
